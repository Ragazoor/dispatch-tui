#!/usr/bin/env bash
# repos.conf — shared repository list for dispatch feed scripts.
#
# Edit this file to configure which GitHub repositories are monitored by
# fetch-dependabot.sh, fetch-security.sh, and fetch-cve.sh.
#
# Add "owner/repo" slugs, one per line:
#
# REPOS=(
#   "myorg/frontend"
#   "myorg/backend"
# )
REPOS=()
