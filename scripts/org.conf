#!/usr/bin/env bash
# org.conf — GitHub organisation list for fetch-reviews.sh's assignee:@me
# query ONLY. This is a separate scope from repos.conf's REPOS list: it does
# not affect the review-requested/reviewed-by/commenter queries, which stay
# scoped to REPOS.
#
# Add "org" slugs, one per line:
#
# ORGS=(
#   "myorg"
#   "another-org"
# )
ORGS=()
